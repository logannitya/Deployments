pipeline {

    agent any

    stages {

        stage("Interactive_Input") {
            steps {
                script {

                   
                    def inputobject
                    

                    // Get the input
                    def userInput = input id: 'UserInput', message: 'Enter Objects:?', parameters: [string(defaultValue: 'None', description: 'object_names', name: 'object', trim: false)]

                    
                    inputConfig = userInput.object?:''
                   

                    // Echo to console
                    echo("Object_name: ${inputobject}")
                  
					
					println inputobject.getClass()
					def test = inputobject.split()
					def new_list = test.toList()
                    println new_list
					
                    
                }
            }
        }
    }
}